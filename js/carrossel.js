// Inicializa o carrossel usando jQuery ou Bootstrap nativo
document.addEventListener('DOMContentLoaded', function() {
    $('.carousel').carousel({
        interval: 3000 // Tempo de troca entre as imagens em milissegundos
    });
});
